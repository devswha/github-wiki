import type { ReactElement } from "react";

export function WikiFooter(): ReactElement {
  return (
    <footer className="wiki-footer">
      <p>Github.wiki scaffold</p>
      <p>Repository articles use local sample data until a content pipeline is connected.</p>
    </footer>
  );
}
